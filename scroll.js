document.getElementById("scrollHome").addEventListener("click", function () {
    window.scrollTo({ top: 0, behavior: "smooth" }); // Scroll to the top of the page
});

document.getElementById("scrollPortfolio").addEventListener("click", function () {
    window.scrollTo({ top: 1000, behavior: "smooth" }); // Scroll to the top of the page
});

document.getElementById("scrollContact").addEventListener("click", function () {
    window.scrollTo({ top: 5500, behavior: "smooth" }); // Scroll to the top of the page
});


